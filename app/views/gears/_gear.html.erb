<li>
  <span class="title"><%= gear.title %></span>
  <% if current_user?(gear.user) %>
    <%= link_to "delete", gear, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: gear.title %>
  <% end %>
</li>